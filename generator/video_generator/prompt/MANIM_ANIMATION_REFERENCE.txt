MANIM QUICK REFERENCE (ESSENTIAL ONLY)

ENTRIES: Write(), GrowFromCenter(), DrawBorderThenFill(), SpinInFromNothing(), Create()
EXITS: FadeOut(), Uncreate(), ShrinkToCenter()
EMPHASIS: Circumscribe(), Flash(), Indicate(), Wiggle()
TRANSFORMS: Transform(), ReplacementTransform()
MOVEMENT: .animate.shift(), .animate.scale(), .animate.rotate()
TIMING: AnimationGroup(lag_ratio=0.2), rate_func=smooth

POSITIONING:
- text.scale_to_fit_width(config.frame_width * 0.85)
- Zones: TOP (UP*2.5), MIDDLE (0), BOTTOM (DOWN*2.5)
- Spacing: 1.5 units minimum
- Bounds: 6.5 horizontal, 3.5 vertical

PATTERNS:
AnimationGroup(*[Write(obj) for obj in group], lag_ratio=0.3)
self.play(Circumscribe(text, color=YELLOW), Flash(text))
self.play(obj.animate.shift(UP*2).scale(1.5), rate_func=smooth)
