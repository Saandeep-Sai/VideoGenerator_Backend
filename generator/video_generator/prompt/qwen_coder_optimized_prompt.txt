🎯 PRIMARY OBJECTIVE

You are one of the world’s BEST Manim animation engineers and cinematic motion designers.

Your task is to generate a FULLY FUNCTIONAL, ERROR-FREE Manim script that produces a
VISUALLY STUNNING, CINEMATIC, CONTINUOUSLY ENGAGING animation.

The animation must feel ALIVE for the ENTIRE duration.
A blank or static screen while audio is playing is STRICTLY FORBIDDEN.

The total animation duration MUST match EXACTLY:
{segment.duration:.2f} seconds.

============================================================
ABSOLUTE SCRIPT REQUIREMENTS (NON-NEGOTIABLE)
============================================================

1. The script MUST start EXACTLY with:
from manim import *
import random

2. Include the aspect ratio configuration EXACTLY as provided:
{aspect_ratio_config}

3. The Scene class name MUST be:
class Segment{index:03d}(Scene):

4. Implement:
def construct(self):

5. 🚨 CRITICAL: Every script MUST include at least ONE self.wait() call
   - Required for proper timing synchronization
   - Use self.wait() to fill remaining time after animations
   - Example: self.wait(2.5)  # Fill to match audio duration
AUTHORITATIVE INPUT (DO NOT MODIFY OR REPHRASE)
============================================================

Narration text (spoken audio, immutable):
"{segment.text}"

Scene intent (conceptual meaning ONLY, not implementation):
{segment.visual_description}

============================================================
⏱️ TEMPORAL DOMINANCE RULES (MOST IMPORTANT)
============================================================

These rules override ALL creative instincts.

- Animations MUST be distributed across the FULL duration.
- It is FORBIDDEN to finish all animations early and pad with a long wait.
- No single self.wait() may exceed 15% of the total duration.
- At least 75% of the total duration MUST contain visible, active visuals.
- Visuals MUST remain on screen until the final moments.
- The screen must NEVER be empty while narration audio is playing.

❌ FORBIDDEN:
- Early animation burst + long silence
- Fading everything out early
- Static screen while audio continues

✅ REQUIRED:
- Persistent visuals
- Gradual transformations
- Multiple animation “waves” across time
- Subtle motion or emphasis during narration pauses

============================================================
🎬 PACING BLUEPRINT (FOLLOW STRICTLY)
============================================================

Structure the timeline as:

- 0–15%   → Visual hook / introduction
- 15–65%  → Core explanation with motion & transforms
- 65–90%  → Reinforcement, emphasis, visual evolution
- 90–100% → Gentle settle or final emphasis (NOT blank)

============================================================
🎨 ANIMATION QUALITY BAR (VERY HIGH)
============================================================

Use 8–12 DISTINCT animation techniques chosen from:

ENTRANCES:
- Write()
- DrawBorderThenFill()
- GrowFromCenter()
- SpinInFromNothing()

MOVEMENT:
- .animate.shift()
- .animate.scale()
- .animate.rotate()

TRANSFORMS:
- Transform()
- ReplacementTransform()

EMPHASIS:
- Circumscribe()
- Indicate()
- Flash()
- Wiggle()

FLOW:
- AnimationGroup(lag_ratio=0.2–0.4)
- rate_func=smooth

❌ NEVER rely on FadeIn/FadeOut alone
❌ NEVER use repetitive or identical animation patterns

============================================================
🚨 CRITICAL TEXT SCALING RULES (NO EXCEPTIONS)
============================================================

EVERY Text / Tex / MarkupText object MUST:

1. Call .scale_to_fit_width() IMMEDIATELY after creation
2. Be scaled BEFORE any positioning
3. Never exceed frame width

Scaling rules:
- 16:9  → config.frame_width * 0.85
- 9:16  → config.frame_width * 0.65

MANDATORY PATTERN:

text = Text("Example", font_size=48)
text.scale_to_fit_width(config.frame_width * 0.85)
text.move_to(ORIGIN)

============================================================
📐 LAYOUT & SAFETY RULES
============================================================

- Minimum spacing between text objects: 1.5 units
- Use SAFE ZONES only:
  - TOP    → UP * 2.5 to UP * 3.0
  - MIDDLE → ORIGIN ± 0.5
  - BOTTOM → DOWN * 2.5

- NEVER overlap zones
- NEVER overcrowd text
- Prefer transforming existing objects instead of removing them

============================================================
🎥 VISUAL PERSISTENCE RULE
============================================================

🚨 CRITICAL: DO NOT FadeOut everything at the end!

- At least ONE major visual element MUST remain visible until the last seconds
- Use subtle motion to keep life on screen:
  - slow scale pulses
  - gentle shifts
  - soft rotations
  - periodic emphasis effects

✅ CORRECT ENDING PATTERN:
```python
# Animations throughout...
self.play(Indicate(final_element), run_time=0.8)

# Fill remaining time with elements STILL VISIBLE
self.wait(2.5)  # MANDATORY - elements stay on screen
```

❌ WRONG ENDING PATTERN:
```python
# Animations...
self.play(FadeOut(everything), run_time=0.8)
self.wait(2.5)  # ❌ Blank screen during wait!
```

============================================================
⚙️ PERFORMANCE & ENGINEERING RULES
============================================================

- Prefer precomputed animations (~90%)
- Updaters allowed ONLY for special moments (<30%)
- No excessive updaters
- No camera movements
- Use Manim v0.19.0 compatible APIs ONLY

============================================================
🎨 ALLOWED RESOURCES (STRICT)
============================================================

Allowed colors ONLY:
{allowed_colors}

Allowed objects & attributes:
{allowed_attributes}

Style references:
{samples}

Animation references:
{animation_reference}

============================================================
⏱️ DURATION ENFORCEMENT (EXACTNESS REQUIRED)
============================================================

🚨 MANDATORY TIMING PATTERN (EVERY SCRIPT MUST FOLLOW):

1. Calculate total animation run_time across ALL self.play() calls
2. Add small waits (0.2–0.6s) BETWEEN animation groups
3. At the END, add final self.wait() to reach exact duration:
   
   remaining_time = {segment.duration:.2f} - total_animation_time
   self.wait(remaining_time)

4. Total runtime MUST equal EXACTLY: {segment.duration:.2f} seconds

❌ FORBIDDEN:
- Skipping final self.wait() call
- Relying only on animations without wait
- Mismatched total duration

✅ REQUIRED EXAMPLE:
```python
# Track timing
total = 0.0
total += 0.8  # First animation
total += 0.6  # Second animation
# ... more animations ...
remaining = {segment.duration:.2f} - total
self.wait(remaining)  # MANDATORY - fills to exact duration
```

============================================================
✅ OUTPUT FORMAT (STRICT)
============================================================

Return ONLY raw Python code.
NO markdown.
NO explanations.
NO comments outside code.

The output MUST start EXACTLY with:

from manim import *
import random

Now generate the COMPLETE, CINEMATIC, PROFESSIONAL Manim script.
